{% extends 'EtkAdminBundle::admin.layout.html.twig' %}

{% block content %}
    {{ parent() }}
    <!-- Page Content -->
    <div class="container">
        <div class="row">
                <div class="panel-title">    Lista de usuarios: </div>
                <div class="col-lg-12">
                <div>
                    {%  for user in users %}
                        <div><b>{{ user.Id }}</b> -  {{ user.Nombre }} {{ user.Apellido }}</div>
                    {%  endfor  %}
                </div>
                <a href="{{ path('etk_admin_newUser') }}">Nuevo Usuario</a></li>
            </div>
        </div>
    </div>
{% endblock %}